describe('number.prototype', function () {
  it("This method must return the value 'wtf' for any given context.", function() {
    expect().to.be.equal();
  });
  it("This method must take a function as an argument and call that function against each element in the Array.", function() {
    expect().to.be.equal();
  });
  it("THis method take your array functions that you wrote in ArrayFun and add them to the Array.prototype", function() {
    expect().to.be.equal();
  });
});